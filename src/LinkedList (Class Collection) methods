Вот список всех методов, которые доступны для использования со связанным списком (LinkedList) в Java:

### 1. Методы добавления элементов:

- add(E e): добавить элемент в конец списка;

- add(int index, E element): добавить элемент по указанному индексу;

- addFirst(E e): добавить элемент в начало списка (аналогично push);

- addLast(E e): добавить элемент в конец списка (аналогично add).

### 2. Методы удаления элементов:

- clear(): удалить все элементы из списка;

- remove(): удалить первый элемент списка;

- remove(int index): удалить элемент по указанному индексу;

- remove(Object o): удалить первое вхождение указанного элемента;

- removeFirst(): удалить первый элемент списка (аналогично pop);

- removeLast(): удалить последний элемент списка;

- removeFirstOccurrence(Object o): удалить первое вхождение указанного элемента.

### 3. Методы получения элементов:

- get(int index): получить элемент по указанному индексу;

- getFirst(): получить первый элемент списка;

- getLast(): получить последний элемент списка;

- peek(): получить первый элемент списка без его удаления (возвращает null для пустого списка);

- peekFirst(): получить первый элемент списка без его удаления (возвращает null для пустого списка);

- peekLast(): получить последний элемент списка без его удаления (возвращает null для пустого списка);

- element(): получить первый элемент списка без его удаления (выбрасывает исключение NoSuchElementException для пустого списка).

### 4. Методы проверки списка:

- size(): получить количество элементов в списке;

- isEmpty(): проверить, является ли список пустым;

- contains(Object o): проверить, содержится ли указанный элемент в списке.

### 5. Методы работы со списком:

- clone(): создать копию списка;

- toArray(): преобразовать список в массив;

- iterator(): получить итератор для обхода списка;

- descendingIterator(): получить итератор для обхода списка в обратном порядке;

- listIterator(): получить итератор для обхода списка с возможностью добавления и удаления элементов;

- listIterator(int index): получить итератор для обхода списка начиная с указанного индекса с возможностью добавления и удаления элементов;

- subList(int fromIndex, int toIndex): получить подсписок элементов списка от fromIndex до toIndex (не включительно).

