Некоторые из наиболее часто используемых методов в PriorityQueue в Java:

1. add(E e) - добавляет элемент в очередь. Если очередь заполнена, она может выбросить исключение.

2. offer(E e) - добавляет элемент в очередь. В случае, если очередь заполнена, метод просто возвращает false без выбрасывания исключения.

3. peek() - возвращает наивысший приоритетный элемент, но не удаляет его из очереди. Если очередь пуста, этот метод возвращает значение null.

4. poll() - возвращает наивысший приоритетный элемент и удаляет его из очереди. Если очередь пуста, этот метод также возвращает значение null.

5. remove(Object o) - удаляет заданный элемент из очереди, если он присутствует. Если элемент не найден, метод просто возвращает false.

6. size() - возвращает количество элементов в очереди.

7. clear() - удаляет все элементы из очереди.

8. iterator() - возвращает итератор, который может быть использован для обхода элементов в очереди.

9. toArray() - возвращает массив, содержащий все элементы в очереди, в том же порядке, в каком они находятся в очереди.

